// Toast notifications are handled globally via react-hot-toast.
// Call toast.success('msg'), toast.error('msg'), toast.loading('msg') anywhere.
// The <Toaster /> is mounted in App.jsx.
// This file is kept for import consistency but does not render anything.
export default null;
